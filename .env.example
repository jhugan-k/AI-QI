# Copy to .env and fill in. .env is gitignored — never commit real keys.

# --- Database (matches docker-compose.yml) ---
DATABASE_URL=postgresql://aiqi:aiqi_dev_password@localhost:5433/aiqi

# --- Data sources (register for free keys) ---
# https://data.gov.in  -> profile -> API key
CPCB_API_KEY=
# https://openweathermap.org -> API keys (free tier)
OPENWEATHER_API_KEY=
